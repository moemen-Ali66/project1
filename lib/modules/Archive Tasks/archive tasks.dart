import 'package:flutter/material.dart';

class archive_tasks extends StatelessWidget {
  const archive_tasks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('archive_tasks',style: TextStyle(fontSize:50 ),));
  }
}
